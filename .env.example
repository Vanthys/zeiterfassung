# Application Configuration
PORT=5000
NODE_ENV=production

# Security
JWT_SECRET=your-secret-key-change-this-in-production

# Frontend API URL (used at build time - baked into frontend bundle)
VITE_API_URL=https://zeiterfassung.skumantz.dev

# Frontend URL (for CORS) - Update if deploying to different domain
FRONTEND_URL=https://zeiterfassung.skumantz.dev

# Database will be stored in ./data/time_tracking.db
# This is mounted as a volume in docker-compose.yml
